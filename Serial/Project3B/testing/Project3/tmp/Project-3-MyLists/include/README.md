Here we're implementing a doubly linked list.

`Particle.h` is the data structure describing a single particle.

`ParticleListEntry` is the "node" data structure

`ParticleList` is the linked list implementation